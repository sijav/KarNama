# Full GraphQL endpoint; this address is public and bundled with the app.
VITE_API_URL=http://localhost:4000/graphql
VITE_AUTH_MODE=live
# Use demo explicitly for offline UI work. Demo codes do not grant API access.
