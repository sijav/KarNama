# Full GraphQL endpoint; this address is public and bundled with the app.
VITE_API_URL=http://localhost:4000/graphql
VITE_AUTH_MODE=demo
# Mock login is the temporary default. Use live to enable server authentication.
# Demo codes do not grant API access.
